<view class="page-container safe-bottom">
  <!-- Logo Area -->
  <view class="logo-area">
    <view class="logo-icon">
      <text class="logo-emoji">🍊</text>
    </view>
    <text class="logo-title">小柚英语</text>
    <text class="logo-subtitle">让英语口语练习更简单</text>
  </view>

  <!-- Login Form -->
  <view class="form-area">
    <view class="input-wrapper {{usernameFocus ? 'input-wrapper-focus' : ''}} mb-24">
      <image class="input-icon" src="/images/icon-user.png" mode="aspectFit" />
      <input
        placeholder="请输入用户名"
        placeholder-class="input-placeholder"
        value="{{username}}"
        bindinput="onUsernameInput"
        bindfocus="onUsernameFocus"
        bindblur="onUsernameBlur"
      />
    </view>

    <view class="input-wrapper {{passwordFocus ? 'input-wrapper-focus' : ''}} mb-32">
      <image class="input-icon" src="/images/icon-lock.png" mode="aspectFit" />
      <input
        type="password"
        placeholder="请输入密码"
        placeholder-class="input-placeholder"
        value="{{password}}"
        bindinput="onPasswordInput"
        bindfocus="onPasswordFocus"
        bindblur="onPasswordBlur"
      />
    </view>

    <button class="btn btn-primary btn-block mb-24 {{loading ? 'btn-disabled' : ''}}" bindtap="handleLogin" disabled="{{loading}}">
      登录
    </button>

    <button class="btn btn-gold btn-block mb-32" bindtap="handleWechatLogin">
      微信一键登录
    </button>

    <view class="wechat-note text-center mb-24">
      <text class="text-hint">首次微信登录将自动注册</text>
    </view>

    <view class="text-center">
      <text class="link-text" bindtap="goToRegister">没有账号？立即注册</text>
    </view>
  </view>
</view>
