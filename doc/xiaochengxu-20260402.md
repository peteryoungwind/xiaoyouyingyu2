# 小柚英语微信小程序系统需求设计文档

> 文档日期：2026-04-02
> 适用对象：产品、设计、前后端开发、测试、运维
> 参考依据：
> - `/Users/xiyou/code/xiaoyouyingyu/doc/addStudyFeature-20260401.md`
> - `/Users/xiyou/code/xiaoyouyingyu/doc/addMembershipFeature-20260402.md`
> - `/Users/xiyou/code/xiaoyouyingyu/CLAUDE.md`
> - 当前项目代码与既有 MySQL 数据结构

---

## 1. 文档目标

本文档基于现有系统能力与已有规划文档，整理出**微信小程序版本的系统需求设计方案**。本次调整后的原则如下：

1. 小程序前端页面风格要保持整体一致，建立统一视觉与交互规范。
2. 小程序后端不依赖当前 Java 服务，需由小程序项目配套的新后端独立实现全部业务能力。
3. 新后端底层数据继续使用现有 MySQL 数据库，并适配已有表结构与历史数据。
4. 小程序要完整承接主题、学习中心、会员、卡密等核心业务。
5. 小程序需支持微信号一键登录；若使用一键登录的用户尚未注册，系统应自动完成注册。

---

## 2. 当前系统现状梳理

## 2.1 当前业务现状

现有系统已经验证了以下业务模块：

1. **主题内容系统**：话题列表、搜索、详情、标签、日历、统计。
2. **学习中心系统**：热身、词汇、表达、任务、AI 点评。
3. **会员系统**：会员有效期、卡密兑换、购买/续费引导、后台会员管理。
4. **管理系统**：主题管理、用户管理、卡密管理、AI 模型管理。

这些业务逻辑说明需求方向已经明确，小程序版本不再重新摸索业务，而是要做一套独立可运行的移动端系统与独立后端实现。

---

## 2.2 小程序技术建设原则调整

### 原方案
- 复用 Java Spring Boot 后端
- 小程序作为新增客户端

### 调整后方案
- **小程序前端独立实现**
- **小程序后端独立实现**
- **底层继续使用现有 MySQL 数据库**
- **适配现有表结构、历史数据、字段含义与业务规则**

### 结论
本项目实际要建设的是：

> 一套面向微信小程序的独立前后端系统，其中后端不依赖 Java 服务，但必须兼容现有 MySQL 数据与业务模型。

---

## 2.3 现有 MySQL 数据资产需要继续复用

小程序新后端仍需对接现有数据库中的核心表，包括但不限于：

### 1. users
用于承载：
- 用户账号
- 密码
- 角色
- 会员到期时间
- 会员来源
- 创建时间

### 1.1 user_wechat_auth（建议新增）
用于承载：
- 用户与微信身份的绑定关系
- `user_id`
- `wechat_open_id`
- `wechat_union_id`
- 创建时间
- 更新时间

推荐策略：
1. `users` 作为唯一用户主体。
2. 微信账号作为登录凭证之一，不单独形成第二套用户体系。
3. 通过 `user_wechat_auth` 维护微信身份绑定关系。
4. 同一微信身份只能绑定一个用户。

### 2. topics
用于承载：
- 主题中英文标题
- 标签
- 主题日期
- 讨论问题
- 创建人
- 创建时间

### 3. ai_models
用于承载：
- AI 模型配置
- API 地址
- API Key
- 模型名
- 是否默认

### 4. redeem_codes
用于承载：
- 卡密编码
- 会员天数
- 状态
- 使用人
- 使用时间
- 备注

### 5. membership_records
用于承载：
- 会员变更流水
- 变更前后时间
- 变更来源
- 管理员操作记录

### 6. 兼容要求
新后端必须：
1. 正确读取现有表结构。
2. 正确兼容已有字段命名和含义。
3. 正确处理已有历史数据。
4. 避免破坏当前 Web 系统已存在的数据。
5. 如需新增表，必须与既有表结构保持一致的命名和外键逻辑。

---

## 3. 微信小程序整体建设目标

## 3.1 业务目标

小程序版本目标如下：

1. 提供统一、轻量、移动端友好的英语口语学习入口。
2. 完整覆盖主题浏览、主题学习、AI 点评、会员转化流程。
3. 支持用户通过卡密或人工引导开通会员。
4. 通过统一风格设计提高产品完整度和品牌感。
5. 通过独立后端保障后续小程序业务可独立演进。

---

## 3.2 产品范围

### 一期建议范围
1. 微信号一键注册/登录
2. 用户名密码登录/注册
3. 首页
4. 主题列表
5. 主题详情
6. 学习中心首页
7. 主题学习页
8. 我的页面
9. 设置页
10. 卡密兑换页
11. 日历页
12. 会员开通引导弹窗/页面
13. 独立后端接口与鉴权系统
14. MySQL 兼容接入

### 二期建议范围
1. 学习记录持久化
2. 我的积累
3. 主题收藏
4. 录音作答
5. AI 追问与复练

### 三期建议范围
1. 在线支付开通会员
2. 小程序运营后台
3. 成就系统
4. 分享裂变

---

## 4. 页面风格统一设计要求

这是本次新增的明确要求，小程序前端必须形成统一设计语言。

## 4.1 统一视觉风格原则

1. **统一主色体系**
   - 主色建议延续品牌蓝色系
   - 会员权益可辅以金色/琥珀色强调

2. **统一圆角体系**
   - 卡片、按钮、输入框、弹窗采用统一圆角规范
   - 不同页面避免风格割裂

3. **统一卡片式布局**
   - 首页、主题列表、学习模块、我的页面均采用卡片化结构
   - 统一阴影、边框、留白规范

4. **统一按钮规范**
   - 主按钮：用于提交、进入学习、登录、兑换等核心动作
   - 次按钮：用于取消、关闭、切换、换一批
   - 危险按钮：仅用于删除类操作（一期用户侧尽量不出现）

5. **统一信息层级**
   - 页面主标题、模块标题、说明文字、标签、辅助文案使用固定字号层级

6. **统一图标风格**
   - 所有图标风格一致，不混用不同视觉体系

7. **统一空状态/加载状态/错误状态**
   - 空状态：统一插画或图标 + 文案 + 按钮
   - 加载状态：统一 loading 组件
   - 错误状态：统一 toast 或错误卡片

---

## 4.2 统一交互规范要求

1. 会员受限场景统一使用同一种弹窗样式。
2. 登录提示统一使用同一种交互。
3. AI 内容生成区统一使用折叠卡片 + 生成按钮 + 换一批按钮。
4. 列表页统一使用下拉刷新 + 上拉加载或“加载更多”。
5. 表单页统一使用一致的输入框、校验提示与提交反馈方式。

---

## 5. 系统角色与权限设计

## 5.1 角色定义

### 游客
可用：
- 首页公开内容
- 主题列表基础信息
- 主题详情基础信息
- 标签、统计、日历

不可用：
- 关键词搜索
- 学习中心
- 卡密兑换
- 我的会员信息

### 普通用户 USER
可用：
- 游客全部功能
- 微信号一键登录
- 用户名密码登录/注册
- 搜索主题
- 查看完整主题详情
- 查看会员状态
- 卡密兑换
- 购买/续费引导

默认不可用：
- 学习中心

### 会员用户
可用：
- 普通用户全部功能
- 学习中心首页
- 主题学习页
- 热身、词汇、表达、任务、AI 点评

会员身份判定：
- 非管理员用户，`membership_expire_at > 当前时间`

### 管理员 ADMIN
可用：
- 所有用户能力
- 后续可进入小程序后台或继续用原后台系统

---

## 5.2 权限规则

1. 游客不能搜索主题。
2. 登录用户可以搜索主题。
3. 只有会员用户和管理员可访问学习中心。
4. 只有登录用户可卡密兑换。
5. 已过期会员访问学习中心时，统一提示续费/开通。

---

## 6. 小程序信息架构

## 6.1 底部导航建议

1. 首页
2. 主题
3. 学习
4. 我的

## 6.2 二级页面

1. 登录页
2. 注册页
3. 主题详情页
4. 日历页
5. 主题学习页
6. 设置页
7. 卡密兑换页
8. 会员开通引导页/弹窗

---

## 7. 功能需求设计

## 7.1 认证与登录

### 功能目标
支持两种登录方式：
1. 用户名密码注册/登录
2. 微信号一键登录

### 微信号一键登录规则
1. 小程序前端调用微信登录能力获取登录凭证。
2. 小程序后端与微信侧完成登录态换取与身份识别。
3. 若该微信身份已绑定系统用户，则直接登录。
4. 若该微信身份尚未注册，则系统自动创建用户并完成登录。
5. 自动注册的新用户同样享受“注册赠送 3 天会员”规则。
6. 自动注册时，系统需生成默认用户资料，并保证账号唯一性。

### 用户名密码账号与微信账号合并策略
推荐采用：`users` 主表 + `user_wechat_auth` 绑定表。

规则如下：
1. 平台以 `users` 作为唯一用户主体，一名用户只保留一条主用户记录。
2. 用户名密码登录、微信号一键登录都只是同一用户的不同登录方式。
3. 首次微信登录且系统中不存在绑定关系时：
   - 自动创建 `users` 记录
   - 自动创建 `user_wechat_auth` 绑定记录
   - 自动完成登录
4. 已绑定微信的用户再次一键登录时：
   - 直接通过 `user_wechat_auth` 找到对应 `user_id`
   - 使用同一用户数据登录
5. 已有用户名密码账号的用户，可在设置页主动绑定微信。
6. 已通过微信自动注册的用户，可后续补设用户名和密码。
7. 严禁通过昵称、用户名相似度等方式自动猜测并合并账号。
8. 同一 `wechat_open_id` 或 `wechat_union_id` 只能绑定一个用户。
9. 微信账号与用户名密码账号的合并必须通过显式绑定流程完成。

### 页面内容
1. 用户名密码登录/注册入口
2. 微信号一键登录按钮
3. 登录失败提示
4. 首次微信登录自动注册说明文案（可选）
5. 账号安全入口：支持后续绑定微信或补设用户名密码

---

## 7.2 首页

### 功能目标
帮助用户快速理解产品价值并进入主要链路。

### 页面内容
1. 品牌 Banner
2. 数据概览
3. 最新主题推荐
4. 热门标签
5. 学习中心入口
6. 会员引导入口

### 特别要求
- 页面样式必须与主题页、学习页、我的页统一。
- 首页卡片风格、按钮风格和标签风格必须复用统一组件。

---

## 7.2 主题列表页

### 页面内容
1. 搜索框
2. 标签筛选
3. 主题卡片列表
4. 加载更多

### 规则
- 游客点击搜索时提示登录
- 卡片样式与首页推荐卡片统一

---

## 7.3 主题详情页

### 页面内容
1. 主题基础信息
2. 讨论问题列表
3. 开始练习入口
4. 会员引导提示

### 规则
- 非会员点击“开始练习”时弹出统一会员引导弹窗
- 会员用户直接进入主题学习页

---

## 7.4 学习中心首页

### 页面内容
1. 页面简介
2. 搜索框
3. 标签筛选
4. 主题学习列表
5. 无权限引导区

### 规则
- 非会员展示统一受限样式
- 风格与首页、主题页一致

---

## 7.5 主题学习页

### 页面结构
1. 主题概览区
2. 模式切换区
3. 热身模块
4. 词汇模块
5. 表达模块
6. 任务模块
7. 作答模块
8. AI 点评模块

### 一期作答方式
- 文字作答

### 二期扩展
- 录音作答
- 语音转文本
- 学习记录
- 积累沉淀

### 风格要求
- 各学习模块采用统一卡片风格
- 模块标题、生成按钮、换一批按钮、空状态风格统一

---

## 7.6 我的页面

### 页面内容
1. 用户信息卡
2. 会员信息卡
3. 功能入口列表
4. 退出登录

### 风格要求
- 会员信息卡风格与开通引导弹窗、设置页会员卡保持一致

---

## 7.7 设置页

### 页面内容
1. 会员状态展示
2. 购买/续费入口
3. 卡密兑换入口
4. 修改密码

---

## 7.8 卡密兑换页

### 页面内容
1. 卡密输入框
2. 兑换按钮
3. 成功提示
4. 失败提示
5. 最新会员到期时间

---

## 7.9 会员引导弹窗 / 页面

### 页面内容
1. 提示文案
2. 管理员联系方式
3. 兑换卡密入口
4. 关闭按钮

### 统一文案要求
- 未开通：请联系管理员开通高级功能
- 已过期：你的高级会员已过期，请联系管理员开通高级功能

---

## 8. 小程序独立后端需求

## 8.1 总体要求

小程序必须建设自己的独立后端服务，不能直接依赖当前 Java 服务来承载业务。

新后端必须实现：
1. 用户注册/登录/鉴权
2. 微信号一键登录与自动注册
3. 主题列表、详情、标签、日历、统计
4. 学习中心热身/词汇/表达/任务/点评
5. 会员状态查询
6. 卡密兑换
7. 管理员联系信息返回
8. 后续扩展的学习记录与收藏能力

---

## 8.2 新后端推荐模块划分

1. 认证模块
2. 用户模块
3. 主题模块
4. 学习中心模块
5. 会员模块
6. 卡密模块
7. AI 服务模块
8. 系统配置模块

---

## 8.3 新后端与 MySQL 适配要求

### 基本要求
1. 继续连接现有 MySQL 数据库。
2. 保持与现有 `users`、`topics`、`ai_models`、`redeem_codes`、`membership_records` 的字段兼容。
3. 保证读取、写入、更新逻辑与现有业务语义一致。
4. 对旧数据兼容，包括历史 `PREMIUM_USER` 数据。

### 适配重点

#### users 表适配
新后端要正确适配：
- `role`
- `membership_expire_at`
- `membership_source`
- `membership_updated_at`
- `created_at`

#### topics 表适配
新后端要正确适配：
- `title`
- `title_zh`
- `tags`
- `event_date`
- `questions`
- `creator_id`
- `created_at`

#### redeem_codes 表适配
新后端要正确适配：
- `code`
- `name`
- `days`
- `expire_at`
- `status`
- `used_by`
- `used_at`
- `remark`
- `created_by`
- `created_at`

#### membership_records 表适配
新后端要正确适配会员流水记录。

---

## 8.4 数据兼容策略

1. 不修改旧字段含义。
2. 若新后端新增字段或新表，需采用向后兼容方案。
3. 若小程序后端与旧 Web 后端并行运行，必须避免数据冲突。
4. 会员判定口径必须统一。
5. 卡密状态判断必须统一。

---

## 9. 接口需求方向

虽然本次不再要求依赖 Java 服务，但新后端在接口设计上可参考当前接口能力。

### 建议保留同类接口语义
1. `/auth/register`
2. `/auth/login`
3. `/auth/password`
4. `/topics`
5. `/topics/{id}`
6. `/topics/tags`
7. `/topics/stats`
8. `/topics/calendar`
9. `/learning/topic/{id}`
10. `/learning/warmup`
11. `/learning/vocabulary`
12. `/learning/expressions`
13. `/learning/tasks`
14. `/learning/review`
15. `/user/membership`
16. `/user/membership-contact`
17. `/redeem-codes/redeem`

这样做有利于需求迁移和多端对齐，但实现逻辑由新后端自行承担。

---

## 10. 非功能需求

## 10.1 风格一致性要求

1. 所有页面遵循统一视觉规范。
2. 所有页面复用统一组件库。
3. 所有弹窗遵循统一布局和按钮规范。
4. 所有列表和卡片遵循统一间距、字号和留白规范。

## 10.2 性能要求

1. 列表页需支持分页或懒加载。
2. AI 请求要有明显 loading 状态。
3. 首页和主题列表首屏需快速可见。

## 10.3 安全要求

1. 新后端需自行实现鉴权逻辑。
2. 密码必须安全存储。
3. 卡密兑换必须事务控制。
4. 敏感配置使用环境变量。

## 10.4 可维护性要求

1. 前端组件统一。
2. 前端页面风格统一。
3. 后端模块边界清晰。
4. 数据层与业务层分离。
5. MySQL 适配层要明确封装，避免表结构变化影响业务层。

---

## 11. 分期建议

## 一期
1. 小程序前端统一 UI 体系
2. 独立后端搭建
3. MySQL 兼容接入
4. 登录注册
5. 主题浏览
6. 学习中心
7. 会员状态
8. 卡密兑换

## 二期
1. 学习记录
2. 我的积累
3. 录音作答
4. 收藏与进度

## 三期
1. 在线支付
2. 运营后台
3. 裂变与成就系统

---

## 12. 结论

调整后的方案不再是“复用 Java 服务的小程序前端”，而是：

> 建设一套风格统一、前后端独立、数据库兼容现有 MySQL 的微信小程序系统。

该方案既能继承现有数据资产，又能为小程序后续独立演进提供更好的技术空间。